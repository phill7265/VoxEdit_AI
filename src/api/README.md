# API

External interface for triggering the pipeline.

Endpoints (planned):
- POST /jobs         — submit a new video editing job
- GET  /jobs/{id}    — poll job status
- GET  /jobs/{id}/result — download export manifest
